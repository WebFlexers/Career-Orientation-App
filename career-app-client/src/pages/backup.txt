{courses.map((course) => {
              return (
                <>
                  <Row className={styles["box"]}>
                    <Col className={styles["title"]}>{course.courseName}</Col>
                    <Col className={styles["grade"]}>
                      <b>{course.grade}</b>
                    </Col>
                  </Row>
                </>
              );
            })}