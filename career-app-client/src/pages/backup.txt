if (semester < 4) {
      if (!isRevision) {
        semester = index;
      } else {
        revisionYear = index;
      }
    } else {
      track = props.userData.track;
      if (!isRevision) {
        semester = index;
      } else {
        revisionYear = index;
      }
    }